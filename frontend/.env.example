# Same-origin when served by Express at / (recommended for production)
VITE_GRAPHQL_HTTP=/graphql
# Leave unset to auto-use ws(s)://<host>/graphql from the browser
# VITE_GRAPHQL_WS=
VITE_TWITCH_CLIENT_ID=your_twitch_client_id
