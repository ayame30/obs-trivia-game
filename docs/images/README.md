# README screenshots

Drop PNG/WebP screencaps here, then reference them from the root [`README.md`](../README.md):

```markdown
![Dashboard](docs/images/dashboard.png)
```

Suggested names: `dashboard.png`, `overlay-questions.png`, `overlay-scoreboard.png`, `settings.png`.

App icon source: [`icon.svg`](icon.svg) → `electron/build/icon.ico` (Windows), `icon.icns` (macOS), `icon.png`, and `frontend/public/favicon.*`.
